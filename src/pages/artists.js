import React from 'react'
import Cards from '../Components/Cards';

const Artists = () => {
    return (
        <div>

            <Cards />
        </div>
    );
};

export default Artists